export function DemoBanner() {
  return (
    <div className="demo-banner" role="status">
      <strong>SIMULATION ONLY</strong> — Mock escrow. No real payments, PSPs, banks, or KYC.
      Identity firewall: alias + OTP mock only — no legal name / passport / SSN.
    </div>
  )
}
